## Packages
leaflet | Required for OpenStreetMap maps
react-leaflet | React bindings for Leaflet
@types/leaflet | TypeScript definitions for Leaflet
framer-motion | Smooth animations for mobile bottom sheets and transitions
zustand | Lightweight state management for complex UI states

## Notes
WebSocket connects to /ws path.
Using OpenStreetMap via Leaflet for map rendering (no API keys required).
App uses 'Outfit' and 'DM Sans' Google Fonts for premium typography.
Need to add Leaflet CSS in index.css.
